% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_hi2}
\alias{plot_hi2}
\title{Plot a compact HI control chart for use in multi-panel layouts}
\usage{
plot_hi2(dat, results, centre, threshold, n_length, ttl_len, ...)
}
\arguments{
\item{dat}{Numeric vector of observations (log2 titre scale).}

\item{results}{List of rule results as returned by \code{\link{hi_rules2}}.}

\item{centre}{Central line value.}

\item{threshold}{One standard deviation unit.}

\item{n_length}{Integer vector of length 8: window length per rule.}

\item{ttl_len}{Total number of time points (used to scale point and line
sizes for readability at small panel sizes).}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Called for its side effect (a plot). Returns \code{NULL} invisibly.
}
\description{
Draws a minimal chart with a fixed y-axis range suitable for tiling many
antigen/serum combinations. Flags a pink background when any rule fires.
}
\examples{
dat <- c(2, 4, 6, 8, 6, 4, 2, 4, 6, 8)
res <- hi_rules2(dat, centre = 4, threshold = 1)
plot_hi2(dat, res[[1]], centre = 4, threshold = 1,
         n_length = c(1, 3, 8, 9, 25, 10, 4, 2), ttl_len = length(dat))
}
