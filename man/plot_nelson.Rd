% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_nelson}
\alias{plot_nelson}
\title{Plot a Nelson-style control chart with rule violations highlighted}
\usage{
plot_nelson(dat, results, centre, threshold, n_length)
}
\arguments{
\item{dat}{Numeric vector of observations.}

\item{results}{List of rule results as returned by \code{\link{hi_rules}}.}

\item{centre}{Central line value.}

\item{threshold}{One standard deviation unit.}

\item{n_length}{Integer vector of length 8 giving the window length for
each rule (used for drawing violation segments).}
}
\value{
Called for its side effect (a plot). Returns \code{NULL} invisibly.
}
\description{
Draws the data as a connected series, adds horizontal lines for the
centre and ±1/2/3 SD boundaries, then overlays coloured points and
line segments for each flagged rule violation.
}
\examples{
dat <- c(2, 4, 6, 8, 6, 4, 2, 4, 6, 8)
res <- hi_rules(dat, centre = 4, threshold = 1)
plot_nelson(dat, res[[1]], centre = 4, threshold = 1,
            n_length = c(1, 3, 8, 9, 25, 10, 4, 2))
}
