% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_hi}
\alias{plot_hi}
\title{Plot an HI control chart with rule violations highlighted}
\usage{
plot_hi(dat, results, centre, threshold, n_length, ...)
}
\arguments{
\item{dat}{Numeric vector of observations (log2 titre scale).}

\item{results}{List of rule results as returned by \code{\link{hi_rules}}.}

\item{centre}{Central line value.}

\item{threshold}{One standard deviation unit.}

\item{n_length}{Integer vector of length 8: window length per rule.}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Called for its side effect (a plot). Returns \code{NULL} invisibly.
}
\description{
Symmetric y-axis version of the control chart, scaled so the centre line
sits at the midpoint. Suitable for a single antigen/serum pair.
}
\examples{
dat <- c(2, 4, 6, 8, 6, 4, 2, 4, 6, 8)
res <- hi_rules(dat, centre = 4, threshold = 1)
plot_hi(dat, res[[1]], centre = 4, threshold = 1,
        n_length = c(1, 3, 8, 9, 25, 10, 4, 2))
}
